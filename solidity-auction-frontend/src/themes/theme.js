import { createTheme } from "@mui/material";

export const theme = createTheme({
  colors: {
    tertiary: {
      dark: "#212529",
      main: "#343a40",
      light: "#495057",
    },
  },
  shape: {
    borderRadius: 12,
  },
});
